<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f5f5f5;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      font-family: Arial, sans-serif;
    }
    .card {
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 500px;
    }
    input {
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2 class="text-center mb-4">Shorten Your URL</h2>

    <!-- Error or Success Message -->
    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger">
        <%= error %>
      </div>
    <% } %>

    <% if (typeof success !== 'undefined') { %>
      <div class="alert alert-success">
        <%= success %>
      </div>
    <% } %>

    <form action="/shorten" method="POST">
      <input class="form-control" type="url" name="originalUrl" placeholder="Enter your URL" required>
      <input class="form-control" type="text" name="customCode" placeholder="Custom code (optional)">
      
      
      
      <input class="form-control" type="number" name="expireDays" placeholder="Expire in X days (optional)">

      <button class="btn btn-primary w-100 mt-2" type="submit">Shorten</button>
    </form>
  </div>
</body>
</html>
